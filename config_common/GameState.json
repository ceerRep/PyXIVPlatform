{
    "state_signature": "8B 05 ?? ?? ?? ?? BE 02 00 00 00",
    "_comment_state_signature": "mov eax,[rip+disp32]; mov esi,2 — RIP target is a global uint32[] timeline array (G), not a struct pointer. See AGENTS.md for cross-references (FFXIVClientStructs / Artisan / GatherBuddyReborn).",
    "state_offset": ["0x02", "0x00"],
    "_comment_state_offset": "0x02 = RIP-relative inst_pos (disp32 start, not a plain offset). G+0x00 = Src[0] = currently-playing ActionTimelineId (consumed by RoleState).",
    "quality_offset": ["0x02", "0x38"],
    "_comment_quality_offset": "0x02 = RIP-relative inst_pos. G+0x38 = Src[14] (15th timeline slot). Empirically reads 1/2/3/4 (Normal/Good/Excellent/Poor) during synthesis, but no static IDA xref proves the field is the condition byte — see AGENTS.md open question. CN/Global both use 0x38; older builds shipped 0x30 in config_common."
}
